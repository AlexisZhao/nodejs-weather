<h2 class="page-header">Check weather</h2>
 {{#if errors}}
      {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
      {{/each}}
{{/if}}
<form method="post" action="/cities/weather">
  <div class="form-group">
<label>City Name</label>
<input type="text" class="form-control" name="cityName" placeholder="eg. Ottawa, CA">
  </div>
  <button type="submit" class="btn btn-default">Search</button>
</form> 
<h3>Weather: {{city1}}  {{temp}}</h3>